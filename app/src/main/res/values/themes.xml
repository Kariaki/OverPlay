<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.overplay" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorSecondary</item>
        <item name="colorPrimaryVariant">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorAcent</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="BackgroundColor">@color/background_white</item>
        <item name="headerColor">@color/headerColor</item>
        <item name="bigTextColor">#59575F</item>
        <item name="smallTextColor">@color/small_text_color</item>
        <item name="indicatorColor">@color/colorPrimary</item>
        <item name="messageBox">@color/messageBoxColorLight</item>
        <item name="chatStatusBar">@color/chatColorLight</item>
        <item name="IconTint">@color/IconTintLight</item>
        <item name="reverseColor">@color/background_white</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="fontFamily">@font/inder</item>

        <item name="searchColor">#E8E8E8</item>
        <item name="homeItemBackground">@color/homeItemBackgroundLight</item>

        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.splashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_design_light</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

</resources>